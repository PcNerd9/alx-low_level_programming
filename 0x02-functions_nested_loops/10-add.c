#include "main.h"

/**
 * add - adds two integers and returns the result
 *@a: the first number
 *@b: the second number
 * Return: the result of the addition
 */
int add(int a, int b)
{
	return (a + b);
}
