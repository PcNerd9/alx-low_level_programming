/**
 * op_add - compute the addition of two numbers
 * @a: the first number
 * @b: the seciond number
 *
 * Return: the result
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - compute the substraction of two numbers
 * @a: the first number
 * @b: the second number
 *
 * Return: the result
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - compute the addition of two numbers
 * @a: the first number
 * @b: the seciond number
 *
 * Return: the result
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - compute the addition of two numbers
 * @a: the first number
 * @b: the seciond number
 *
 * Return: the result
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - compute the addition of two numbers
 * @a: the first number
 * @b: the seciond number
 *
 * Return: the result
 */
int op_mod(int a, int b)
{
	return (a % b);
}
